import 'package:flutter/material.dart';

class AppCardTheme {
  static CardThemeData build() => CardThemeData(
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(5.0)),
        color: Colors.white,
        elevation: 0,
      );
}
